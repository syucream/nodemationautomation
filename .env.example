# Required: Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# Optional: Default Claude model (haiku, sonnet, opus, or full model ID)
# Can be overridden with -m/--model CLI option
CLAUDE_MODEL=haiku

# Optional: n8n API configuration
# Set N8N_API_KEY to enable :validate and :deploy commands
N8N_API_KEY=your-n8n-api-key
N8N_BASE_URL=http://localhost:5678

# Optional: Maximum validation retries before asking for human input (default: 3)
MAX_VALIDATION_RETRIES=3
